<h4><strong>From:</strong> <%= conversation.originator.email %>:</h4>
<p><strong> Replying to: </strong> <%= conversation.subject %>  </p>


<table class="table table-striped" style="table-layout: fixed; width: 100%">
  <tbody>


  <% @message = conversation.messages %>
  <% @message.each do |m| %>
      <% conversation.mark_as_read(current_user) %>

      <tr>

        <% if m.sender != current_user %>
            <div style="display: inline-block">
              <td>

              <div style="margin-left: 5px" class="round-image-50">
                <%= image_tag(m.sender.logo.url(:small) ) %>
              </div>
              <%= link_to m.sender.email, m.sender %></td>
            </div>
      <% else %>
            <div style="display: inline-block;">
            <td>
                <div style="margin-left: 5px" class="round-image-50">
                  <%= image_tag(m.sender.logo.url(:small) ) %>
                </div>
                <div id="cell" style="position: absolute; display: inline-block; vertical-align: bottom">Me</div></td>
            </div>
        <% end %>

        <td style="word-wrap: break-word"> <p><%= m.body %> </p></td>



      </tr>

  <% end %>

  </tbody>
</table>
<%= link_to t('.back', :default => t("helpers.links.back")),
            conversations_path, :class => 'btn'  %>

<!--failed ajax attempt. can't even render partials from here. must figure that out first.-->


<%= link_to "Reply AJAX", {:controller => "messages", :action => "reply", :id => conversation.id, :format => :js},
            :remote => true, :id => 'new_reply_link', :class => "btn btn-danger" %>

<td><%= link_to "Reply", {:controller => "messages", :action => "reply", :id => conversation.id}, :title => "Reply", :class => "btn btn-danger"  %></td>


<div id="replypartial">
</div>

1